apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "astronomy-helmcharts.fullname" . }}-test-connection"
  labels:
    {{- include "astronomy-helmcharts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "astronomy-helmcharts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
