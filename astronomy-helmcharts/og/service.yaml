apiVersion: v1
kind: Service
metadata:
  name: {{ include "astronomy-helmcharts.fullname" . }}
  labels:
    {{- include "astronomy-helmcharts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "astronomy-helmcharts.selectorLabels" . | nindent 4 }}
